<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Success</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: #f4e6d0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            perspective: 1000px;
        }

        .order-success-container {
            background-color: #ffffff;
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(182, 139, 64, 0.2);
            padding: 50px;
            text-align: center;
            width: 90%;
            max-width: 600px;
            position: relative;
            overflow: hidden;
            transform-style: preserve-3d;
            transform: rotateX(10deg) scale(0.9);
            opacity: 0;
            animation: reveal 1s forwards;
        }

        @keyframes reveal {
            to {
                transform: rotateX(0) scale(1);
                opacity: 1;
            }
        }

        .background-texture {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: 
                linear-gradient(45deg, rgba(182, 139, 64, 0.05) 25%, transparent 25%),
                linear-gradient(-45deg, rgba(182, 139, 64, 0.05) 25%, transparent 25%);
            background-size: 50px 50px;
            opacity: 0.5;
            z-index: -1;
        }

        .success-icon {
            font-size: 120px;
            color: #b68b40;
            margin-bottom: 20px;
            text-shadow: 0 10px 20px rgba(182, 139, 64, 0.2);
        }

        .thank-you-text {
            font-size: 36px;
            font-weight: bold;
            color: #b68b40;
            margin-bottom: 15px;
        }

        .order-status {
            font-size: 18px;
            color: #6c757d;
            margin-bottom: 30px;
            line-height: 1.6;
        }

        .btn {
            background-color: #b68b40;
            color: #ffffff;
            padding: 15px 30px;
            font-size: 18px;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            text-decoration: none;
            transition: all 0.3s ease;
            display: inline-block;
            box-shadow: 0 10px 20px rgba(182, 139, 64, 0.3);
        }

        .btn:hover {
            background-color: #9c7535;
            transform: translateY(-5px);
            box-shadow: 0 15px 25px rgba(182, 139, 64, 0.4);
        }

        .footer-links {
            margin-top: 25px;
            font-size: 14px;
        }

        .footer-links a {
            color: #b68b40;
            text-decoration: none;
            margin: 0 10px;
            transition: color 0.3s ease;
        }

        .footer-links a:hover {
            color: #9c7535;
            text-decoration: underline;
        }

        @media (max-width: 600px) {
            .order-success-container {
                padding: 30px;
                width: 95%;
            }

            .success-icon {
                font-size: 80px;
            }

            .thank-you-text {
                font-size: 28px;
            }
        }
    </style>
</head>
<body>
    <div class="order-success-container">
        <div class="background-texture"></div>
        <i class="fas fa-check-circle success-icon"></i>
        <div class="thank-you-text">Order Confirmed!</div>
        <div class="order-status">
            Your order has been successfully received 
            and is being processed with care.
        </div>
        <a href="/loadShop" class="btn">Continue Shopping</a>
        <div class="footer-links">
            <a href="/orderDetails?id=<%= orderData._id %>">View Order details</a>
        </div>
    </div>
</body>
</html>